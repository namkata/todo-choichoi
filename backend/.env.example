# Class-level comment: Example environment configuration for backend
# IM8 password policy note: if user auth is added, ensure passwords follow IM8 policy (min length, complexity, rotation) — not applicable yet.

POSTGRES_DSN=host=localhost user=postgres password=postgres dbname=todo port=5432 sslmode=disable
PORT=8080